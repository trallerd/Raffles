
<%= simple_form_for @user do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :email %>
    <%= f.input :password %>
    <%= f.input :photo %>
    <%= f.input :admin %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, f.object.new_record? ? 'Criar User' : 'Atualizar User', class: 'btn btn-primary btn-lg' %>
  </div>
<% end %>
