
<%= simple_form_for @type do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :description %>
    <%= f.input :inicial_number %>
    <%= f.input :step %>
    <%= f.input :ticket_quantities %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, f.object.new_record? ? 'Create Type' : 'Update Type', class: 'btn btn-primary btn-lg' %>
  </div>
<% end %>
